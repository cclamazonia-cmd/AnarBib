# ci-suites.txt — suites SQL exécutées par le job CI « sql-tests ».
# ---------------------------------------------------------------------------
# Une suite par ligne (chemin relatif à la racine du dépôt). # = commentaire.
# Allowlist EXPLICITE : on n'exécute PAS automatiquement tous les paquet*.sql —
# les anciennes suites (paquet19/24/25/26/A/A1…) supposent des données LIVE et
# échoueraient contre la base reconstruite (baseline + seed synthétique). Une
# suite n'arrive ici que lorsqu'elle a des fixtures dynamiques + est compatible
# du seed (cf. supabase/seed.sql) et signale son bilan « … OK : N/N ».
#
# Convention de bilan attendue (cf. runner) : la suite finit par
#   RAISE EXCEPTION '<NOM> OK : N/N tests passés …'   -> succès
#   RAISE EXCEPTION '<NOM> ECHEC : …'                 -> échec
# (le RAISE sert au rollback des fixtures ; le runner parse le message.)
tests/sql/paquet_cotisation_tests.sql
tests/sql/b14_api_cotisation_autrui_tests.sql
tests/sql/b14_api_gel_global_borne_tests.sql
tests/sql/b14_api_refus_muet_listes_tests.sql
tests/sql/b14_oracle_existence_forme_tests.sql
# B14 paquet 4 : deux invariants de sens OPPOSÉ tenus ensemble — deux fonctions
# du catalogage gardées dans le corps mais laissées exécutables (les révoquer
# donnerait un écran mort), deux fonctions sans appelant en session fermées dans
# le droit. Un correctif qui « uniformiserait » les quatre casse un des deux.
tests/sql/b14_gardes_ecriture_tests.sql
# B14 paquet 5 : « es-tu staff ? » et « où ? » doivent désigner la MÊME
# bibliothèque. 37 fonctions lisent ces deux colonnes de `api.my_access`
# ensemble, dont 24 qui écrivent. Le correctif tient à un mot dans un ORDER BY :
# T3 garde la forme, T1 garde l'effet, et il faut les deux.
tests/sql/my_access_biblio_effective_tests.sql
# Decision du 01/09 : les cinq gardes en WHERE rejoignent DOC-SILENCE-1. Cette
# suite APPELLE les fonctions au lieu de relire leur corps — la garde est un
# predicat levant dans un WHERE, ce qui repose sur le plan choisi. T2 tient
# l'autre moitie (l'admin reseau lit toujours) et T3 le piege symetrique : dans
# d'autres fonctions le meme predicat ELARGIT un acces, et le rendre levant y
# casserait la navigation.
tests/sql/refus_reseau_se_dit_tests.sql
# B14 paquet 6 : meme invariant que b14_oracle_existence_forme, mais par la
# STRUCTURE et non par le motif. Le motif avait ferme 14 fonctions et n'en
# voyait pas 9 autres, qui disent la meme chose avec d'autres mots. Son T1 pose
# la bonne question — reste-t-il DEUX refus distinguables ? — celle qui aurait
# attrape le troisieme message oublie dans fn_attach_received_asset_record.
tests/sql/oracle_existence_ordre_tests.sql
# B14 paquet 9 : la salle des machines (vault, HTTP sortant, corps de cron)
# reste fermee au lectorat. Structurel et non nominatif — seules les DEUX
# exceptions HTTP sont nommees, et le T3 verifie qu elles gardent leur garde.
# Un GRANT distrait sur fn_internal_get_vault_secret donnerait TOUS les secrets
# du vault a tout compte du reseau : c est le T2, et il verifie aussi PUBLIC.
tests/sql/salle_des_machines_tests.sql
# Arbitrage des periodiques aligne sur celui des livres (decision du 01/09,
# preavis envoye aux quatre personnes AVANT le deploiement). T3 tient l autre
# moitie de la decision — cataloguer et signaler restent ouverts — et T4 garde
# que les deux familles refusent dans les memes termes.
tests/sql/arbitrage_periodiques_tests.sql
# B14 paquet 10 : un document restreint ne vit jamais dans un bucket public.
# Le talon de la chaine d acces (RPC-portier -> EF qui signe en service_role)
# est le RANGEMENT : un asset conta_ativa dans un bucket public serait servi
# par l URL publique, sans RPC ni EF, et RIEN ne casserait — le parcours normal
# continuerait de fonctionner. T4 garde la propriete revocable du PEB numerique.
tests/sql/documents_numeriques_tests.sql
# B14 paquet 11 (cloture) : la carte en travail est pour les membres. 79 des
# 187 entrees de cartographie etaient non publiques et visibles de tout compte
# — l inscription etant ouverte, « savoir creer un compte » suffisait a voir ce
# qui peut attendre un consentement. T2 et T3 tiennent les deux bords : le
# compte sans adhesion voit ENCORE la carte publique, le membre voit TOUT.
tests/sql/cartographie_reseau_membres_tests.sql
tests/sql/paquet_depot_garantie_tests.sql
tests/sql/paquet_renouvellement_granulaire_tests.sql
tests/sql/onbo_q13_transfer_mandate_tests.sql
tests/sql/onbo_111_lot1_eval_tests.sql
tests/sql/onbo_111_lot2a_messages_tests.sql
tests/sql/onbo_111_lot3c_digest_tests.sql
tests/sql/paquet_emprestimos_tests.sql
tests/sql/paquet_reservas_tests.sql
tests/sql/invitation_equipe_tests.sql
tests/sql/paquet_peb_ill_lifecycle_tests.sql
tests/sql/reading_notes_rls_tests.sql
tests/sql/reading_notes_lot5_work_per_book_tests.sql
# Cohérence structurelle allowlist fn_enqueue_* ↔ CHECK des *_notification_events.
# Purement catalogue système : ni fixture, ni donnée, ni Vault.
tests/sql/notifications_event_types_coherence_tests.sql

# Reversibilite de l ecart « pas un doublon » (paquet DOUBLONS P3).
# Fixture autonome : ne depend d aucune donnee de demonstration.
tests/sql/doublons_p3_reversibilite_tests.sql

# Arbitrage destructeur reserve a la coordination + signalements (paquet DOUBLONS P4).
# Inclut une fusion reellement executee : le corps de merge_book y est recopie.
tests/sql/doublons_p4_arbitrage_coordination_tests.sql

# Apercu de fusion en lecture seule avant destruction (paquet DOUBLONS P6).
# Verifie le tri entre metadonnees perdues, divergentes et silencieuses.
tests/sql/doublons_p6_apercu_fusion_tests.sql

# Reprise de champs avant fusion : ce qui allait etre perdu peut etre garde
# (paquet DOUBLONS P7). Verifie surtout qu un champ refuse LEVE une erreur.
tests/sql/doublons_p7_reprise_champs_tests.sql

# Signalement de doublons d autorites (paquet DOUBLONS P8) : ce qu on rend au
# staff en echange de merge_author. Le test 1 garde l OUVERTURE du geste.
tests/sql/doublons_p8_signalement_autorites_tests.sql

# Apercu et reprise de champs pour les AUTORITES (paquet DOUBLONS P9) : ce que
# merge_author detruisait en silence — 574 autorites portent des dates.
tests/sql/doublons_p9_apercu_reprise_autorites_tests.sql

# Balayage global des autorites (paquet DOUBLONS P10). Garde surtout la
# coherence avec la detection par autorite (DEDUP-6) et le piege des dates NULL.
tests/sql/doublons_p10_balayage_autorites_tests.sql

# Arbitrage « pas un doublon » pour les autorites (paquet DOUBLONS P11).
# Garde surtout que les DEUX detections excluent les memes paires (DEDUP-6).
tests/sql/doublons_p11_arbitrage_autorites_tests.sql

# Type d autorite et collectivites inversees (CONV-O7). Garde surtout que le
# verdict humain ne soit pas defait par une reouverture de brouillon (test 6)
# ni ecrase quand la fiche a bouge depuis la proposition (test 5).
tests/sql/conv_o7_collectivites_tests.sql

# Scission d autorite (CONV-O8). Garde surtout la cle primaire de book_authors
# quand la fiche composee figure deux fois sur le meme livre (test 6) et la
# copie du nom dans book_contributors (test 8), qui est le plus discret des
# echecs : autorite scindee, livre affichant encore le nom compose.
tests/sql/conv_o8_scission_tests.sql

# Invitation d une bibliotheque, lot 1 : le schema des claims s ouvre a un
# claim sans compte. Garde surtout que la garantie perdue (user_id NOT NULL) est
# REMPLACEE et non abandonnee, et que la revocation mord vraiment.
tests/sql/invitation_claims_lot1_tests.sql

# Periodiques (paquets P1 a P5). Couvre les « Verifications de cloture » de la
# spec : les trois gardes de filiation et de materiel, la cle de designation
# inecrivable, la regle anti-faux-doublons dans les DEUX sens (bruit ecarte,
# doublon de saisie conserve), l etat de collection declare que le calcul ne
# doit jamais ecraser, la fusion (G5 : concatenation marquee, formes rejetees,
# filiation reportee) et la RLS proposto/ativo mesuree en vrai changement de
# role. Ces tests NE PEUVENT PAS vivre dans les migrations : elles s appliquent
# avant le seed, donc sans bibliotheque ni membre.
tests/sql/periodiques_tests.sql

# Invitation lot 2 : emettre, revoquer, lister. Garde surtout QUI peut emettre
# (D2 : admins reseau seuls), que le jeton ne se persiste jamais en clair, et
# que la liste ne fuit ni jeton ni hash.
tests/sql/invitation_claims_lot2_tests.sql

# Invitation lot 3a : note scindee (interne / mot d accompagnement), nom rendu
# a la personne invitee, purge a 45 jours. Garde surtout que la note INTERNE ne
# sorte jamais vers elle, et que la purge n emporte pas ce qu elle ne doit pas.
tests/sql/invitation_claims_lot3a_tests.sql

# Mentions de biblios hors reseau exposees a la coordination (decision C).
# Garde surtout que l ABSENCE de consentement vaut refus, et que le rattachement
# nominatif ne suive pas le consentement au contact.
tests/sql/mentions_orphelines_tests.sql

# Import d un candidat institutionnel (mode Busca). La suite EMPRUNTE le chemin
# au lieu de le relire : elle appelle la RPC avec le JWT d une coordinatrice.
# T5 confronte le match_status a la CHECK elle-meme (et non a une liste
# recopiee) : c est la forme generale du defaut repare. T9 garde que le
# rapprochement est bien appele, faute de quoi un doublon repartirait en
# new_record et l assistant le promouvrait tout seul.
tests/sql/import_candidat_institutionnel_tests.sql

# Le chemin OAI-PMH (paquet chemin-oai-pmh-executable). Les DEUX RPC etaient
# inexecutables ; la suite les APPELLE avec le JWT d'une admin reseau au lieu
# de les relire. T4 et T9 confrontent les quatre statuts ecrits a leurs CHECK
# elles-memes (et non a des listes recopiees) : c'est la forme generale du
# defaut repare. T11 garde la LIMITE du paquet — le verrou in_progress que
# seule l'EF du Lot 3b, absente, saurait relacher.
tests/sql/import_oai_pmh_tests.sql

# Doublons INTERNES a un lot d import (paquet doublons-intra-lot). Le
# rapprochement compare au CATALOGUE, jamais les lignes du lot entre elles :
# le listing Solidaires portait neuf paires du meme ouvrage, toutes sorties
# new_record. T2 et T4 sont des tests de NON-ACTION et sont les plus utiles —
# deux homonymes ne doivent PAS etre signales, et un appariement au catalogue
# ne doit jamais etre ecrase.
tests/sql/import_doublons_intra_lot_tests.sql

# Supprimer un run d'import ne doit pas abandonner son lot de catalogage. Le
# run et ses lignes ingest.* partent en CASCADE ; le lot et ses brouillons
# vivent dans public et restaient derriere — lot 55 (CIRA Marseille), 237
# brouillons sans plus aucun ecran ou lire leur provenance. T2 est un test de
# NON-ACTION : le refus ne doit RIEN detruire. T3 tient l'autre moitie de la
# decision — la corbeille du lot ne bloque pas, sinon on recree l'impasse.
tests/sql/import_suppression_run_lot_tests.sql

# Une suppression definitive de brouillon se trace et se rejoue (levier 2 de la
# portee des gestes destructeurs). T2 et T5 sont des tests de NON-ACTION : une
# mise a la corbeille ne doit PAS remplir le journal de faux positifs, et
# rejouer deux fois doit etre refuse — sinon un double clic fabrique un doublon
# a partir de l'instantane. T4 tient la raison d'etre du trigger BEFORE : apres,
# les enfants en CASCADE n'existent plus.
tests/sql/catalogage_suppression_tracee_tests.sql

# Portee des gestes de catalogage : ou atterrit un livre publie, et qui peut
# supprimer definitivement. T2 est un test de NON-ACTION — une adhesion REVOQUEE
# et de role "reader" (celle que l'ancien tri alphabetique preferait) ne doit pas
# designer la bibliotheque de destination. T6 tient le piege du DELETE refuse par
# RLS : il ne leve pas d'erreur, il supprime zero ligne.
tests/sql/portee_catalogage_tests.sql

# Les droits herites (TRUNCATE / REFERENCES / TRIGGER a anon et authenticated)
# ne reviennent pas. Les retirer une fois ne sert a rien : le defaut du schema
# les redonne a chaque table neuve, et celui pose par supabase_admin n'est pas
# modifiable depuis une migration. T2 est le plus important et ne parle pas de
# ces trois droits : il garde a zero la classe REELLEMENT dangereuse, une table
# ecrivable sans RLS. T3 est un test de non-action — un REVOKE trop large
# passerait T1 et T2 sans broncher.
tests/sql/grants_herites_tests.sql

# Ce qu'un lot contient, et les vues qui le disent. T1 est un test de NON-ACTION
# et porte tout le paquet : un lot VIDE doit apparaitre a 0/0, sinon il est
# invisible a l'ecran et jamais montre comme supprimable. T4 surveille une derive
# qui se produit toute seule — le defaut du schema redonne a anon et authenticated
# SELECT/INSERT/UPDATE/DELETE sur toute vue neuve, et une vue simple est
# auto-modifiable, donc un DELETE la traverse jusqu'a la table de base.
tests/sql/lots_comptes_tests.sql

# `grants_herites_tests.sql` garde `public`, et son T2 dit le bon critere : une
# table ecrivable sans RLS. Mais il ne regarde que `public`, et c'est par la que
# `ingest` est passe au travers pendant des mois - huit tables sans RLS, jamais
# relevees, sur des donnees de bibliotheques tierces. Cette suite est le meme
# controle, pour le schema qui manquait. T2 est le plus important et ne parle pas
# de securite : FORCE ROW LEVEL SECURITY couperait les dix-sept fonctions d'import
# d'un coup, silencieusement, et il a l'air d'un durcissement. T6 garde
# l'invariant qui rend la RLS sans policy transparente pour l'import : tables et
# fonctions au meme proprietaire.
tests/sql/ingest_ferme_tests.sql

# Les six suites ci-dessous etaient presentes au depot et absentes de ce
# manifeste : elles ne tournaient pas. Ce sont les plus anciennes, et elles
# couvrent le coeur de la circulation — emprunts, consultations, profils de
# bibliotheque. Une suite presente mais non executee est pire qu'une suite
# absente : elle donne l'impression que le domaine est couvert.
# Reactivees le 29/08/2026 apres verification que les 40 objets qu'elles
# appellent existent tous encore en base. Cela ne dit pas qu'elles passent :
# si l'une rougit, c'est l'information qu'on cherchait, et sql-tests n'est pas
# une barriere de deploiement.
tests/sql/paquetA_profils_tests.sql
tests/sql/paquetA1_cancel_note_required_tests.sql
tests/sql/paquet19_loan_wrappers_tests.sql
tests/sql/paquet24_consulta_helpers_tests.sql
tests/sql/paquet25_consulta_wrappers_tests.sql
tests/sql/paquet26_consulta_notification_triggers_tests.sql

# Le T7 de grants_herites garde le meme invariant sur `public` : aucune vue
# qui echappe aux policies ne doit etre lisible. Il ne regardait pas `api`,
# ou sept vues etaient dans ce cas. T2 est le plus important : les deux vues
# de gouvernance restent volontairement hors policies — en invoker, la
# jointure sur profiles renverrait NULL a l'admin qui doit decider — et leur
# visibilite tient a une seule clause dans la vue. Un CREATE OR REPLACE
# distrait la ferait disparaitre sans bruit.
tests/sql/vues_api_definer_tests.sql

# Le trigger est l invariant : il couvre TOUS les chemins de creation d une
# biblio la ou patcher la seule RPC de constitution n en couvre qu un. T3 est
# le test qui compte — il prouve que les valeurs posees sont exactement le
# repli deja applique par le code, donc que la migration ne change aucun envoi.
tests/sql/canal_invariant_tests.sql

# B16 : le calcul du slug sort du corps de fn_provision_preactive_library pour
# etre testable seul. T5 est le garde-fou qui compte sur la duree — il refuse
# qu on reinsere le calcul dans le corps, ce qui reintroduirait le defaut sans
# qu aucun voyant ne rougisse.
tests/sql/slug_biblioteca_tests.sql

# B12 : un envoi non effectué dit pourquoi. T4 à T7 ECRIVENT — vérifier que la
# colonne existe ne prouve rien, ce qui compte est que la base REFUSE une ligne
# sautée sans raison. T8 est le garde-fou sur la durée.
tests/sql/outbox_raison_du_saut_tests.sql

# F4 : les rappels d'échéance existent enfin. T6 ÉCRIT — il vérifie qu'un même
# rappel ne peut pas partir deux fois, ce qu'une contrainte déclarée ne prouve
# pas. T5 refuse que l'ancien mi-parcours revienne à côté du nouveau.
tests/sql/rappels_echeance_tests.sql

# Les crons attendus sont planifiés, et eux seuls. Suite rendue possible le
# 31/08/2026 par tests/sql/_ci_setup_cron_stub.sql : jusque-là la base de test
# n'avait pas de schéma `cron`, donc AUCUN des trente-six crons de production
# n'était couvert. Elle énonce ce que produit le DÉPÔT rejoué, pas l'état de la
# production — son bilan le redit à chaque passage.
tests/sql/crons_planifies_tests.sql

# Saut collégial reader -> coordenador (GOUV-11/GOUV-12, 01/09/2026) :
# réglage OFF par défaut, reader actif strict, circuit complet, revérification
# à l'acceptation, non-régression de l'échelle. Réf. 20260901162610.
tests/sql/saut_collegial_tests.sql

# L'accueil aussi est collégial (GOUV-13, 01/09/2026) : la promotion directe
# reader -> librarian lève collegiality_required, le circuit d'invitation
# est le seul chemin. Réf. 20260901175233.
tests/sql/t1_accueil_collegial_tests.sql

# Morte et anonyme : aucune des deux (B20 premier geste, 02/09/2026) : les
# trois wrappers de planification de retour sans aucun appelant ne sont plus
# exécutables par anon ; authenticated inchangé tant que B20 n'a pas arbitré
# les lots. Réf. 20260902092104.
tests/sql/surface_morte_anon_fermee_tests.sql

# La carte se taille, elle ne s'efface qu'a la main (B20 lot cartographie,
# 02/09/2026) : create_entry (jamais servie, les 187 entrees viennent d'uMap)
# et toggle_public (redondante d'update_self et update_admin) fermees ;
# delete reste ouverte — branchee le meme jour dans CartographyEditModal.
# Ref. 20260902095935.
tests/sql/cartographie_surface_taillee_tests.sql

# Un instrument de console n'a pas besoin de la porte du navigateur (B20 lot
# conventions, 02/09/2026) : conv_controle_qualite / conv_controle_resumo
# fermees a anon+authenticated ; l'instrument reste entier (vues private
# presentes, postgres garde EXECUTE). Ref. 20260902100917.
tests/sql/conventions_console_fermee_tests.sql
