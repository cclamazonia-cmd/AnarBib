---
description: Audit i18n AnarBib : scan residuel, parite des 10 locales, conformite a la charte inclusive. Ne corrige pas en place.
---
Tu vas auditer l'internationalisation d'AnarBib.

Contexte fige : 10 locales = ca, de, el, en, eo, es, fr, it, nl, pt-BR. pt-BR = locale par defaut. Fichiers de locale PLATS (cles type "account.loans.renew"), LF sans BOM, indentation 2 espaces.

Etapes :
1. Lance le scan des chaines residuelles (texte en dur non internationalise) : `node scan_i18n_residual.js` (ajuste le chemin si besoin). Montre-moi les residus trouves.
2. Verifie la PARITE des 10 locales : toute cle presente dans une locale doit exister dans les 9 autres. Liste les cles manquantes, par locale.
3. Lis la charte `notes-audit/anarbib-charte-langage-inclusif-v1.md` (source unique de verite) et verifie la conformite des libelles inclusifs : e neutre argentin (es), point median (fr), Genderstern + tutoiement (de), asterisque (it), triple forme o/a/e (pt-BR), epicene (en). REGLE DURE : jamais "camerata" / "camerati".
4. Signale-moi les ecarts (residus, cles manquantes, libelles non conformes). NE MODIFIE RIEN en place.
5. Si je valide une correction, livre-moi les fichiers de locale COMPLETS, deja patches (pas d'instructions a coller). Pour AJOUTER des cles : methode .cjs additive purement textuelle (insertion avant le } final), idempotente, puis validation `node -e "JSON.parse(require('fs').readFileSync(path,'utf8'))"`. JAMAIS ConvertFrom-Json + reserialisation (ca transforme les \n et casse le JSON).
