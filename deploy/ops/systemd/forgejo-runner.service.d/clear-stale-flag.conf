# Ajoute le 30/07 : efface le drapeau d echec runner au (re)demarrage avec Docker
# joignable (wait-for-docker deja passe). Evite que ~/.bashrc reaffiche une vieille
# alerte apres une reprise. Prefixe - = non bloquant.
[Service]
ExecStartPre=-/usr/bin/rm -f /home/accattone/anarbib-ops/.last-failure-runner
