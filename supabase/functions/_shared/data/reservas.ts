import type { ReservaWorkflowItem } from "../core/types.ts";
import { supabaseAdmin } from "../core/env.ts";
export async function getReservaDetalhes(id:number) { const {data,error}=await supabaseAdmin.from("v_reservas_detalhes").select("*").eq("reserva_id",id).maybeSingle(); if (error) throw error; if (!data) throw new Error("Reserva não encontrada."); return data as Record<string,unknown>; }
export async function getReservaV2Bundle(id:number) {
  const {data:reserva,error:e1}=await supabaseAdmin.from("reservas_v2").select("id,user_id,library_id,created_at,updated_at,notes,status_global").eq("id",id).maybeSingle(); if (e1) throw e1; if (!reserva) throw new Error("Reserva não encontrada.");
  const {data:profile,error:e2}=await supabaseAdmin.from("profiles").select("id,email,first_name,last_name,phone,address,consent_email,preferred_language").eq("id",reserva.user_id).maybeSingle(); if (e2) throw e2; if (!profile) throw new Error("Perfil não encontrado.");
  const {data:items,error:e3}=await supabaseAdmin.schema("api").from("reserva_itens_followup_ui").select("reserva_id,line_no,sub_id,bib_ref,autor,titulo,editora,ano,item_status,expires_at,cancelled_at,expired_at").eq("reserva_id",id).order("line_no",{ascending:true}); if (e3) throw e3;
  return {reserva:reserva as Record<string,unknown>,profile:profile as Record<string,unknown>,items:(items||[]) as Record<string,unknown>[]};
}
export async function getReservaWorkflowBundle(id:number,lineNos?:number[]) {
  const {data:reserva,error:e1}=await supabaseAdmin.from("reservas_v2").select("id,user_id,library_id,created_at,updated_at,notes,status_global").eq("id",id).maybeSingle(); if (e1) throw e1; if (!reserva) throw new Error("Reserva não encontrada.");
  const {data:profile,error:e2}=await supabaseAdmin.from("profiles").select("id,email,first_name,last_name,phone,address,consent_email,preferred_language").eq("id",reserva.user_id).maybeSingle(); if (e2) throw e2; if (!profile) throw new Error("Perfil não encontrado.");
  let q=supabaseAdmin.schema("api").from("reserva_itens_followup_ui").select("reserva_id,user_id,line_no,sub_id,bib_ref,autor,titulo,rotulo,item_status,workflow_stage_effective,workflow_note,pickup_scheduled_for,pickup_reply_status,pickup_reply_note,pickup_reply_at").eq("reserva_id",id).order("line_no",{ascending:true}); if (lineNos?.length) q=q.in("line_no",lineNos); const {data:items,error:e3}=await q; if (e3) throw e3;
  return {reserva:reserva as Record<string,unknown>,profile:profile as Record<string,unknown>,items:((items||[]) as Record<string,unknown>[]).map(it=>({line_no:Number(it.line_no||0)||undefined,sub_id:String(it.sub_id||"").trim()||null,bib_ref:String(it.bib_ref||"").trim()||null,titulo:String(it.titulo||"").trim()||null,autor:String(it.autor||"").trim()||null,rotulo:String(it.rotulo||"").trim()||null,item_status:String(it.item_status||"").trim()||null,workflow_stage_effective:String(it.workflow_stage_effective||"").trim()||null,workflow_note:String(it.workflow_note||"").trim()||null,pickup_scheduled_for:String(it.pickup_scheduled_for||"").trim()||null,pickup_reply_status:String(it.pickup_reply_status||"").trim()||null,pickup_reply_note:String(it.pickup_reply_note||"").trim()||null,pickup_reply_at:String(it.pickup_reply_at||"").trim()||null}))};
}
